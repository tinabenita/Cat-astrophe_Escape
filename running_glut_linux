sudo apt-get install freeglut3 freeglut3-dev
vi file.c
------------------------------------------------
Command to run road1.c:

gcc -o road1 road1.c -lGL -lGLU -lglut
./road

-------------------------------------------------
Command to run cat.c:

gcc -o cat cat.c -lGL -lGLU -lglut -lm
./cat

